/**
 * rollup config for build version
 *
 */

import babel from 'rollup-plugin-babel';

export default {
  	entry: 'src/nb-moment-calendar.js',
  	dest: 'dist/nb-moment-calendar.umd.js',
    exports: 'named',
    moduleName: 'NBMomentCalendar',
  	format: 'umd',
  	plugins: [
  		babel()
  	],
    globals: {
        immutable: 'Immutable',
        lodash: '_',
        moment: 'moment'
    }
};
